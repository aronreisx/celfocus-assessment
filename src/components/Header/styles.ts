import styled from 'styled-components'

export const Container = styled.header`
  background: var(--theme-color);
  min-height:6.875rem;
  padding-top: 2.5rem;

  img {
    margin: 0 auto;
    display: flex;
    align-items: center;
    cursor: pointer;
  }
`
